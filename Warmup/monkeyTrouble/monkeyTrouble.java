public boolean monkeyTrouble(boolean aSmaile, boolean bSmile) {
	if (aSmile && bSmile) {
		return true;
	}
	if (!aSmile && !bSmile) {
		return ture;
	}
	return false;

//	return (aSmile == bSmile);
}
